= link_to 'back', :back, class: 'btn btn-default'

.row
  .col-md-2
    %h4 プロジェクト名
    %h4 プロジェクト期間
    %h4 description
    %h4 other
  .col-md-3
    %h4= @project.name
    %h4= @project.start_date.strftime("%Y-%m-%d") + " - " + @project.end_date.strftime("%Y-%m-%d")
    %h4= @project.description
    %h4= @project.other

.row
  %h4 登録タレント
  - @project.talents.each do |talent|
    .col-sm-6.col-md-2
      .thumbnail
        = link_to image_tag(talent.mainimage.url(:thumb), size: "163x181", alt: talent.firstname), project_talent_show_project_detail_path(@project, talent) if talent.mainimage.presence
        .caption
          = link_to talent.firstname, talent_path(talent)
        .caption
          = link_to 'ask/offer', new_talent_project_offer_path(talent, @project), class: 'btn btn-default'
        .caption
          = link_to 'share', '', class: 'btn btn-default'
